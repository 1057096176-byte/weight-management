# 导航功能测试指南

## 🎯 快速测试步骤

### 1️⃣ 首页导航测试 (/)

启动应用后，首页应该显示：
- ✅ 顶部："健康助手"标题和菜单按钮
- ✅ 医生问候卡片
- ✅ 状态卡片（新用户/无方案/有方案）
- ✅ 常见问题快捷按钮
- ✅ 底部功能入口栏

**测试操作：**
1. 点击左上角菜单按钮 → 应该打开侧边栏
2. 点击"开始智能导诊"按钮 → 应该跳转到 `/triage`
3. 点击底部"今日打卡"按钮 → 应该跳转到 `/checkin`
4. 点击底部"智能导诊"按钮 → 应该跳转到 `/triage`
5. 点击底部"数据"按钮 → 应该跳转到 `/data`

### 2️⃣ 侧边栏导航测试

在任意页面：
1. 点击左上角菜单按钮打开侧边栏
2. 测试以下链接：
   - ✅ 消息 → 跳转到首页 `/`
   - ✅ 智能导诊 → 跳转到 `/triage`
   - ✅ 今日打卡 → 跳转到 `/checkin`
   - ✅ 数据与预警 → 跳转到 `/data`
   - ✅ 个人中心 → 跳转到 `/profile`
   - ✅ 李医生/王医生 → 跳转到 `/doctor/1` 或 `/doctor/2`

**预期行为：**
- 点击任何链接后，侧边栏应该自动关闭
- 页面应该平滑切换到目标页面

### 3️⃣ 智能导诊流程测试 (/triage)

**进入方式：**
- 从首页点击"开始智能导诊"
- 从侧边栏点击"智能导诊"
- 从底部快捷栏点击"智能导诊"

**测试流程：**
1. ✅ 页面加载后显示欢迎消息
2. ✅ 点击快捷选项按钮（如"男"、"女"等）
3. ✅ 输入文字回复并发送
4. ✅ 完成所有三个阶段（基础信息、症状评估、风险评估）
5. ✅ 查看推荐科室和医生分身卡片
6. ✅ 点击医生卡片 → 应该跳转到 `/doctor/:id`
7. ✅ 点击左上角返回按钮 → 应该返回首页

**状态保存测试：**
1. 开始导诊但不完成
2. 点击返回首页
3. 确认弹窗提示并返回
4. 重新进入导诊页 → 应该恢复之前的进度

### 4️⃣ 医生问诊页面测试 (/doctor/:id)

**进入方式：**
- 从导诊页点击推荐医生卡片
- 从侧边栏点击"李医生"或"王医生"

**测试操作：**
1. ✅ 查看医生信息卡片
2. ✅ 发送消息与医生对话
3. ✅ 点击"查看方案详情" → 应该跳转到 `/plan`
4. ✅ 点击左上角返回 → 应该返回上一页

### 5️⃣ 体重管理方案页面 (/plan)

**进入方式：**
- 从医生问诊页点击"查看方案详情"
- 直接访问 `/plan`

**测试操作：**
1. ✅ 查看方案详情
2. ✅ 测试暂停/恢复方案功能
3. ✅ 点击返回按钮 → 返回上一页

### 6️⃣ 打卡中心测试 (/checkin)

**进入方式：**
- 从首页点击"去打卡"
- 从侧边栏点击"今日打卡"
- 从底部快捷栏点击"今日打卡"

**测试操作：**
1. ✅ 切换不同打卡类型（全部、体重、饮食、运动）
2. ✅ 填写打卡信息
3. ✅ 点击提交打卡
4. ✅ 点击返回按钮 → 返回上一页

### 7️⃣ 数据分析页面 (/data)

**进入方式：**
- 从首页点击"查看数据"
- 从侧边栏点击"数据与预警"
- 从底部快捷栏点击"数据"

**测试操作：**
1. ✅ 切换时间周期（7天、30天、90天）
2. ✅ 查看不同数据图表
3. ✅ 查看健康预警信息
4. ✅ 点击返回按钮 → 返回上一页

### 8️⃣ 个人中心测试 (/profile)

**进入方式：**
- 从侧边栏点击"个人中心"

**测试操作：**
1. ✅ 查看个人信息
2. ✅ 点击编辑按钮
3. ✅ 查看隐私政策等
4. ✅ 点击返回按钮 → 返回上一页

### 9️⃣ 404页面测试

**测试方法：**
1. 在浏览器地址栏输入不存在的路径
   - 例如：`/test123`
   - 例如：`/abc`
   - 例如：`/doctor/999/test`

**预期行为：**
- ✅ 显示404页面，带有友好的提示
- ✅ 显示"返回首页"和"返回上一页"按钮
- ✅ 点击"返回首页" → 跳转到 `/`
- ✅ 点击"返回上一页" → 使用浏览器历史记录返回

### 🔟 浏览器导航测试

**测试浏览器自带的前进/后退功能：**
1. 依次访问：首页 → 导诊 → 医生问诊 → 方案详情
2. 点击浏览器后退按钮 × 3 → 应该依次返回到首页
3. 点击浏览器前进按钮 × 3 → 应该重新到达方案详情页
4. 刷新任意页面 → 应该保持在当前页面

## ✅ 通过标准

所有导航功能正常工作，符合以下标准：

1. **点击响应** - 所有链接和按钮都能正常响应点击
2. **页面跳转** - 能够正确跳转到目标页面
3. **URL变化** - 浏览器地址栏URL正确更新
4. **返回功能** - 后退按钮能够返回上一页
5. **状态保持** - 页面状态在导航后正确保持或恢复
6. **侧边栏关闭** - 从侧边栏导航后自动关闭
7. **无错误提示** - 控制台没有路由相关错误

## ❌ 常见问题排查

### 问题1: 点击链接没反应
**可能原因：**
- RouterProvider 未正确配置
- Link 组件导入错误

**解决方法：**
1. 检查 `/src/app/App.tsx` 是否使用 RouterProvider
2. 确认所有 Link 导入自 `react-router`

### 问题2: 页面刷新后404
**可能原因：**
- 开发服务器路由配置问题

**解决方法：**
- 通常在生产环境需要配置服务器支持SPA路由

### 问题3: 控制台报错 "Illegal constructor"
**可能原因：**
- 使用了 `react-router-dom` 而不是 `react-router`

**解决方法：**
- 确保所有导入都来自 `react-router` 包

## 📊 测试检查清单

使用此清单逐项测试：

- [ ] 首页显示正常
- [ ] 侧边栏打开/关闭正常
- [ ] 侧边栏所有链接可用
- [ ] 底部快捷操作栏所有链接可用
- [ ] 智能导诊流程完整
- [ ] 导诊进度能够保存和恢复
- [ ] 医生卡片跳转正常
- [ ] 医生问诊页显示正常
- [ ] 方案详情页显示正常
- [ ] 打卡页面功能正常
- [ ] 数据页面图表显示正常
- [ ] 个人中心显示正常
- [ ] 404页面显示正常
- [ ] 浏览器前进/后退功能正常
- [ ] 页面刷新保持当前路由
- [ ] 控制台无路由错误

---

**测试完成后，所有功能应该都能正常工作！** 🎉
