# 首页功能入口栏 - 横向滚动设计

## 设计特点

### 1. 横向滚动布局
- ✅ 支持左右滑动查看更多功能
- ✅ 自动隐藏滚动条，保持界面美观
- ✅ 流畅的滑动体验

### 2. 按钮设计
**布局：左图标 + 右文字**
```
┌─────────────────┐
│ 🔵 [图标] 功能名 │
└─────────────────┘
```

**视觉效果：**
- 圆角胶囊形状（rounded-full）
- 渐变色背景（每个功能不同颜色）
- 图标在半透明白色圆形背景中
- 白色文字
- 悬停时阴影加深

### 3. 功能入口列表（8个）

| 图标 | 名称 | 颜色 | 跳转页面 |
|------|------|------|---------|
| 📅 | 今日打卡 | 蓝色渐变 | /checkin |
| 🩺 | 智能导诊 | 紫色渐变 | /triage |
| 📊 | 数据 | 绿色渐变 | /data |
| 🎯 | 我的方案 | 橙色渐变 | /plan |
| 👤 | 医生分身 | 粉色渐变 | /doctor/1 |
| 🔔 | 预警 | 红色渐变 | /data |
| 📖 | 健康知识 | 靛蓝渐变 | /profile |
| ⚙️ | 设置 | 灰色渐变 | /profile |

## 技术实现

### CSS 关键类
```css
.overflow-x-auto     // 横向滚动
.scrollbar-hide      // 隐藏滚动条
.flex-shrink-0       // 防止按钮被压缩
.whitespace-nowrap   // 文字不换行
```

### 响应式处理
- 在小屏幕上，用户可以滑动查看所有功能
- 在大屏幕上，可能显示更多按钮（取决于屏幕宽度）
- 最大宽度限制为 4xl（max-w-4xl）

### 隐藏滚动条
使用自定义样式支持所有浏览器：
```css
.scrollbar-hide {
  -ms-overflow-style: none;  /* IE/Edge */
  scrollbar-width: none;      /* Firefox */
}
.scrollbar-hide::-webkit-scrollbar {
  display: none;              /* Chrome/Safari */
}
```

## 用户体验

### 交互提示
- **视觉引导**：超出屏幕的内容会在边缘显示一部分，提示用户可以滑动
- **平滑滚动**：原生滚动体验，支持触摸和鼠标滚轮
- **即时反馈**：悬停时阴影变化

### 滑动行为
- 👆 **触摸设备**：手指左右滑动
- 🖱️ **桌面设备**：
  - 鼠标拖拽（按住并移动）
  - 鼠标滚轮（Shift + 滚轮）
  - 触控板左右滑动

## 布局结构

```
首页布局
├── 顶部导航栏
├── 主内容区（可滚动）
│   ├── 医生形象和问候卡片
│   ├── 状态卡片
│   ├── 常见问题
│   └── 聊天消息
├── 功能入口栏（横向滚动）← 新设计
│   ├── [图标] 今日打卡
│   ├── [图标] 智能导诊
│   ├── [图标] 数据
│   ├── [图标] 我的方案
│   ├── [图标] 医生分身
│   ├── [图标] 预警
│   ├── [图标] 健康知识
│   └── [图标] 设置
└── 底部输入框
```

## 扩展性

### 添加新功能入口
在 `QuickActions.tsx` 的 `actions` 数组中添加：

```typescript
{
  icon: YourIcon,           // Lucide 图标
  label: "功能名称",         // 显示文字
  to: "/your-path",         // 跳转路径
  bgColor: "bg-gradient-to-r from-color-500 to-color-600", // 渐变色
}
```

### 颜色系统
使用 Tailwind 的渐变色类：
- 蓝色：`from-blue-500 to-blue-600`
- 紫色：`from-purple-500 to-purple-600`
- 绿色：`from-green-500 to-green-600`
- 橙色：`from-orange-500 to-orange-600`
- 粉色：`from-pink-500 to-pink-600`
- 红色：`from-red-500 to-red-600`
- 靛蓝：`from-indigo-500 to-indigo-600`
- 灰色：`from-gray-500 to-gray-600`

## 与侧边栏的区别

| 功能 | 功能入口栏 | 侧边栏 |
|------|-----------|--------|
| 位置 | 底部，输入框上方 | 左侧，折叠展开 |
| 交互 | 横向滚动 | 垂直滚动 |
| 显示 | 始终可见 | 点击菜单按钮显示 |
| 内容 | 核心快捷功能 | 完整功能 + 历史记录 |
| 适用场景 | 快速跳转 | 详细导航 |

## 设计理念

1. **可发现性**：将常用功能放在显眼位置
2. **可访问性**：一键直达，减少操作步骤
3. **空间利用**：横向滚动最大化功能数量
4. **视觉美观**：渐变色彩区分不同功能
5. **操作便捷**：大按钮，易于点击

## 移动端优化

- 按钮大小适合手指点击（最小44x44像素）
- 间距合理，避免误触
- 支持原生滑动手势
- 无需学习成本

## 未来优化建议

1. **个性化排序**：根据用户使用频率动态排序
2. **徽章提示**：显示未读数量、待办事项
3. **滑动指示器**：小圆点显示当前位置
4. **快捷操作**：长按显示更多选项
5. **自定义**：允许用户添加/移除功能
