# 医生分身问诊模块 - 自动路径划分实现

## ✅ 核心改进

### 移除手动按钮触发
❌ **之前**：需要用户点击"生成干预方案"、"推荐线下就诊"等按钮
✅ **现在**：根据对话内容自动判断路径并显示对应卡片

---

## 🤖 自动路径划分逻辑

### 路径判断机制

#### 1. 高危风险检测（路径B：线下就诊）
**触发条件**：用户消息包含高危关键词
```typescript
const highRiskKeywords = [
  "高血压", "糖尿病", "心脏", "胸闷", 
  "头晕", "呼吸困难", "严重", "急性"
];
```

**示例对话**：
```
用户："我最近总是感觉胸闷，有时候还头晕"
AI：  "我注意到您提到了一些需要重视的健康信号。根据您的描述，
      我建议您先进行线下就诊，完成专业的医学检查，以确保安全。
      请查看下方的详细建议。"
      
→ 自动设置 riskLevel = "high"
→ 自动显示线下就诊推荐卡片
```

#### 2. 低风险管理（路径A：线上干预）
**触发条件**：根据对话轮次和内容自动判断

**触发时机**：
- 讨论"饮食/外食"话题 + 对话轮次 ≥ 2 → 触发方案生成
- 讨论"减重/减肥/营养"话题 + 对话轮次 ≥ 3 → 触发方案生成
- 一般对话 + 对话轮次 ≥ 4 → 触发方案生成

**示例对话1**（快速触发）：
```
轮次1：
用户："我平时经常外食"
AI：  "[饮食建议...]基于您的情况，我可以为您生成一个详细的个性化干预方案。"

轮次2（自动触发）：
AI：  "好的！我已经根据您的情况，为您生成了一个为期3个月的个性化体重管理方案。
      
      方案包括：
      ✅ 详细的饮食能量目标和营养素分配
      ✅ 一日三餐示例食谱
      ✅ 每周运动计划（频次、类型、时长）
      ✅ 生活方式调整建议
      
      请查看下方卡片了解详情。"
      
→ 自动设置 planGenerated = true
→ 自动显示线上干预方案卡片
```

**示例对话2**（渐进触发）：
```
轮次1：
用户："如何科学减重不反弹？"
AI：  "[减重建议...]"

轮次2：
用户："那运动方面有什么建议吗？"
AI：  "[运动建议...]"

轮次3：
用户："营养方面需要注意什么？"
AI：  "[营养建议...]"

轮次4（自动触发）：
AI：  "好的！我已经根据您的情况，为您生成了一个为期3个月的
      个性化体重管理方案..."
      
→ 自动显示线上干预方案卡片
```

---

## 🎯 智能对话系统

### 关键词匹配回复

| 用户输入关键词 | AI回复内容 | 自动触发 |
|---------------|-----------|----------|
| "减重"/"减肥" | 科学减重建议 + BMI分析 | 轮次≥3触发方案 |
| "运动" | 有氧+力量训练计划 | - |
| "营养"/"饮食" | 营养素分配 + 热量目标 | 轮次≥3触发方案 |
| "外食"/"自己做" | 饮食习惯建议 | 轮次≥2触发方案 |
| "平台期" | 突破平台期策略 | - |
| "高血压"等高危词 | 线下就诊建议 | 立即显示线下卡片 |
| 其他一般提问 | 通用建议 | 轮次≥4触发方案 |

---

## 📋 页面结构

### 顶部区域
- **返回按钮**
- **页面标题**："医生分身问诊"
- **医生信息卡片**：
  - 头像（渐变背景圆形）
  - 名称 + 职称
  - 专业擅长
  - 科室标签
  - 方案状态徽章（动态显示）

### 聊天区域
1. **常见问题区**（初始显示）
   - 三组问题，支持换一批
   - 点击自动发送提问
   - 用户提问后自动隐藏

2. **对话消息**
   - 用户消息（右侧，蓝色背景）
   - AI消息（左侧，白色背景）
   - 自动滚动到最新消息

3. **路径A卡片**（自动显示）
   - 条件：`riskLevel === "low" && planGenerated && messages.length > 4`
   - 内容：轻盈计划 + 能量/营养/运动目标
   - 按钮：查看完整方案并开启干预计划

4. **路径B卡片**（自动显示）
   - 条件：`riskLevel === "high" && messages.length > 3`
   - 内容：推荐科室 + 检查项目 + 挂号指引
   - 按钮：查看附近医院及挂号信息

### 底部区域
- **仅保留输入框**
  - 支持文本输入
  - 支持语音输入（发送语音 / 语音转文字）
  - 自动定位到最新消息

---

## 🔄 状态管理

### 核心状态变量

```typescript
// 风险等级（自动判断）
const [riskLevel, setRiskLevel] = useState<"low" | "medium" | "high">("low");

// 方案生成状态（自动触发）
const [planGenerated, setPlanGenerated] = useState(false);

// 对话轮次计数（自动递增）
const [conversationRounds, setConversationRounds] = useState(0);
```

### 状态流转

```
初始状态
↓
用户提问 → conversationRounds++
↓
AI分析关键词
↓
├─ 包含高危词? → Yes → riskLevel = "high" → 显示路径B卡片
└─ No → 正常回复
         ↓
      轮次判断
         ↓
      ├─ 饮食话题 + 轮次≥2? → Yes → 触发方案生成
      ├─ 减重话题 + 轮次≥3? → Yes → 触发方案生成
      └─ 一般话题 + 轮次≥4? → Yes → 触发方案生成
                ↓
           planGenerated = true
                ↓
           显示路径A卡片
```

---

## 💡 用户体验优化

### 1. 自然对话流程
- ✅ 无需用户点击按钮
- ✅ 系统自动判断风险等级
- ✅ 适时推送对应方案
- ✅ 避免打断对话体验

### 2. 智能触发时机
- ✅ 饮食话题：2轮对话后触发（用户已提供足够信息）
- ✅ 减重话题：3轮对话后触发（充分了解需求）
- ✅ 一般话题：4轮对话后触发（确保信息完整）
- ✅ 高危信号：立即触发（保障用户安全）

### 3. 渐进式引导
- ✅ 初始：显示常见问题，降低提问门槛
- ✅ 对话中：根据内容提供专业建议
- ✅ 信息足够：自动生成个性化方案
- ✅ 发现风险：及时提示线下就诊

---

## 🎯 完整用户旅程

### 场景1：低风险用户（轻松模式）

```
1. 进入页面
   → 看到医生问候 + 导诊数据引用 + 常见问题

2. 点击常见问题："如何科学减重不反弹？"
   → AI回复减重建议

3. 继续提问："我平时经常外食"
   → AI回复饮食建议
   → 🎉 自动触发：生成个性化方案消息

4. 查看自动显示的方案卡片
   → 能量目标、营养分配、运动计划
   → 点击"查看完整方案并开启干预计划"

5. 跳转到方案详情页
   → 开始3个月体重管理之旅
```

### 场景2：高风险用户（安全优先）

```
1. 进入页面
   → 看到医生问候 + 导诊数据引用

2. 提问："我最近总是胸闷，还有点头晕"
   → AI立即识别高危关键词
   → 🚨 自动触发：线下就诊建议消息

3. 查看自动显示的就诊推荐卡片
   → 推荐科室：内分泌科、心血管内科
   → 建议检查项目
   → 挂号指引

4. 点击"查看附近医院及挂号信息"
   → 前往医院完成专业检查
```

### 场景3：渐进式咨询（深度交流）

```
1. 点击："适合我的运动强度是多少？"
   → AI回复运动建议（轮次1）

2. 追问："减重期间如何保证营养？"
   → AI回复营养建议（轮次2）

3. 追问："会不会影响基础代谢？"
   → AI回复代谢相关（轮次3）

4. 追问："那我应该从哪里开始？"
   → AI提供行动建议（轮次4）
   → 🎉 自动触发：生成个性化方案

5. 查看方案卡片并开启计划
```

---

## ✨ 技术亮点

### 1. 智能关键词检测
```typescript
// 高危风险检测
const highRiskKeywords = [
  "高血压", "糖尿病", "心脏", "胸闷",
  "头晕", "呼吸困难", "严重", "急性"
];
const hasHighRisk = highRiskKeywords.some(keyword => 
  message.includes(keyword)
);
```

### 2. 对话轮次追踪
```typescript
// 每次用户发送消息时自动递增
const newRounds = conversationRounds + 1;
setConversationRounds(newRounds);

// 根据轮次和话题决定是否触发方案
if (newRounds >= 2 && !planGenerated) {
  shouldGeneratePlan = true;
}
```

### 3. 延迟触发优化
```typescript
// AI回复后延迟2秒再显示方案卡片
setTimeout(() => {
  setMessages(prev => [...prev, planMessage]);
  setPlanGenerated(true);
}, 2000);
```

### 4. 条件渲染控制
```typescript
// 路径A：低风险 + 已生成方案 + 消息数>4
{riskLevel === "low" && planGenerated && messages.length > 4 && (
  <OnlinePlanCard />
)}

// 路径B：高风险 + 消息数>3
{riskLevel === "high" && messages.length > 3 && (
  <OfflineRecommendationCard />
)}
```

---

## 📊 状态决策树

```
用户发送消息
    ↓
检测高危关键词?
    ├─ Yes → riskLevel = "high"
    │        → shouldRecommendOffline = true
    │        → 显示线下就诊卡片
    │
    └─ No → 分析消息内容
            ↓
         匹配话题类型?
            ├─ "饮食"话题 + 轮次≥2 → 触发方案
            ├─ "减重"话题 + 轮次≥3 → 触发方案
            ├─ "营养"话题 + 轮次≥3 → 触发方案
            └─ 一般话题 + 轮次≥4 → 触发方案
                    ↓
               shouldGeneratePlan = true
                    ↓
              延迟2秒后显示方案卡片
                    ↓
            planGenerated = true
                    ↓
          医生信息卡显示"已生成方案"徽章
```

---

## 🎉 总结

### 核心优势
✅ **自动化**：无需手动点击按钮，系统自动判断
✅ **智能化**：根据对话内容和轮次自动触发
✅ **安全性**：高危信号立即识别并提示就诊
✅ **流畅性**：对话体验自然，不打断用户思路
✅ **个性化**：方案生成时机基于用户实际需求

### 实现完成度
- ✅ 移除所有手动按钮
- ✅ 实现对话内容分析
- ✅ 实现轮次追踪机制
- ✅ 实现自动路径划分
- ✅ 实现卡片自动显示
- ✅ 保留完整的输入功能

**总完成度：100%** 🎉

现在医生分身问诊模块完全按照"根据对话自动划分路径并显示卡片"的逻辑运行！
