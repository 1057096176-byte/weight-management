# 医生分身问诊 - 测试指南

## ✅ 已修复：卡片显示条件

### 修改前
```typescript
// ❌ 条件太严格
{riskLevel === "low" && planGenerated && messages.length > 4 && (<Card />)}
{riskLevel === "high" && messages.length > 3 && (<Card />)}
```

### 修改后  
```typescript
// ✅ 简化条件，立即显示
{riskLevel === "low" && planGenerated && (<Card />)}
{riskLevel === "high" && (<Card />)}
```

---

## 🧪 快速测试方法

### 测试路径A（线上干预方案）

#### 方法1：快速触发（2轮对话）
1. 进入医生分身页面
2. 点击快捷选项："经常外食"
3. **等待3.5秒** → 自动显示方案生成消息
4. **立即显示** → 蓝紫色渐变方案卡片

#### 方法2：渐进触发（3轮对话）
1. 点击常见问题："如何科学减重不反弹？"
2. 再点击："适合我的运动强度是多少？"
3. 再点击："减重期间如何保证营养？"
4. **第3轮后** → 自动显示方案卡片

#### 方法3：输入关键词
输入以下任一关键词：
- "我想减重"
- "如何减肥"
- "饮食怎么调整"
- "营养怎么搭配"

**对话3轮后** → 自动触发方案生成

---

### 测试路径B（线下就诊推荐）

#### 立即触发（1轮对话）
输入以下任一高危关键词：
- "我最近总是胸闷"
- "我有高血压"
- "我有糖尿病"
- "经常头晕"
- "心脏不舒服"
- "呼吸困难"

**1.5秒后** → AI回复建议就诊
**2.5秒后** → 立即显示红色就诊推荐卡片

---

## 📊 卡片显示逻辑

### 路径A卡片（蓝紫渐变）
**显示条件**：
```typescript
riskLevel === "low" && planGenerated
```

**触发时机**：
- 饮食话题 + 对话轮次 ≥ 2
- 减重话题 + 对话轮次 ≥ 3
- 营养话题 + 对话轮次 ≥ 3
- 一般话题 + 对话轮次 ≥ 4

**卡片内容**：
- 轻盈计划标题
- 能量与体重目标
- 宏量营养素分配
- 运动计划
- "查看完整方案并开启干预计划"按钮

---

### 路径B卡片（红橙渐变）
**显示条件**：
```typescript
riskLevel === "high"
```

**触发时机**：
- 用户消息包含高危关键词
- 立即设置 `riskLevel = "high"`
- AI回复后1秒显示卡片

**卡片内容**：
- 建议线下就诊标题
- 推荐科室（内分泌科、心血管内科）
- 建议检查项目
- 挂号/预约指引
- "查看附近医院及挂号信息"按钮

---

## ⏱️ 完整时间线

### 路径A时间线（最快触发）
```
0:00 - 用户输入："经常外食"
0:00 - 用户消息显示
1:30 - AI回复饮食建议
1:30 - 判断：newRounds=1, 对话轮次≥2? ✅
3:30 - 显示："好的！我已经根据您的情况..."
3:30 - setPlanGenerated(true)
3:30 - 🎉 方案卡片立即显示
```

### 路径B时间线（立即触发）
```
0:00 - 用户输入："我最近胸闷"
0:00 - 用户消息显示
1:30 - AI回复："我注意到您提到了一些需要重视的健康信号..."
1:30 - setRiskLevel("high")
2:30 - 🚨 就诊推荐卡片立即显示
```

---

## 🔍 调试检查清单

如果卡片没有显示，检查以下项：

### ✅ 路径A卡片不显示
- [ ] `planGenerated` 是否为 `true`？
- [ ] `riskLevel` 是否为 `"low"`？
- [ ] 是否看到方案生成消息？
- [ ] 对话轮次是否达到触发条件？

### ✅ 路径B卡片不显示
- [ ] `riskLevel` 是否为 `"high"`？
- [ ] 用户消息是否包含高危关键词？
- [ ] 是否看到AI的就诊建议消息？

### ✅ 控制台检查
打开浏览器开发者工具，查看：
```javascript
// 在控制台输入
console.log(riskLevel)  // 应该是 "low" 或 "high"
console.log(planGenerated)  // 应该是 true（路径A）
console.log(conversationRounds)  // 对话轮次
```

---

## 🎯 推荐测试流程

### 完整测试（5分钟）

**测试1：快速触发路径A**
1. 刷新页面
2. 点击："经常外食"
3. 等待4秒
4. ✅ 确认看到蓝紫色方案卡片

**测试2：触发路径B**
1. 刷新页面
2. 输入："我有高血压"
3. 等待3秒
4. ✅ 确认看到红色就诊推荐卡片

**测试3：渐进式对话**
1. 刷新页面
2. 依次点击3个常见问题
3. 第3个问题后等待4秒
4. ✅ 确认看到方案卡片

**测试4：状态持久性**
1. 触发方案生成后
2. 继续提问
3. ✅ 确认卡片保持显示
4. ✅ 确认医生卡片显示"已生成方案"徽章

---

## ✨ 预期效果

### 视觉效果
- ✅ 卡片从下方滑入（opacity + y动画）
- ✅ 渐变背景平滑过渡
- ✅ 自动滚动到卡片位置
- ✅ 医生信息卡片更新状态徽章

### 交互效果
- ✅ 卡片按钮可点击
- ✅ 跳转功能正常
- ✅ 卡片显示后不再消失
- ✅ 继续对话不影响卡片显示

---

## 🐛 常见问题

### Q: 为什么点击"经常外食"后没有立即显示卡片？
**A**: 需要等待3.5秒：
- 1.5秒：AI回复延迟
- 2秒：方案生成消息延迟
- 卡片紧随方案消息显示

### Q: 为什么对话多轮后才显示卡片？
**A**: 这是设计行为，确保收集足够信息：
- 饮食话题：2轮后触发（快）
- 减重/营养话题：3轮后触发（中）
- 一般话题：4轮后触发（慢）

### Q: 如何重置状态重新测试？
**A**: 刷新页面或点击返回按钮后重新进入

---

## 🎉 验收标准

### ✅ 路径A验收
- [ ] 点击"经常外食"后4秒内显示方案卡片
- [ ] 卡片内容完整（能量、营养、运动）
- [ ] "已生成方案"徽章出现
- [ ] 点击卡片按钮可跳转到方案页面

### ✅ 路径B验收
- [ ] 输入"高血压"后3秒内显示就诊卡片
- [ ] 卡片内容完整（科室、检查、指引）
- [ ] 红色警告样式正确
- [ ] 点击卡片按钮有响应

### ✅ 整体体验验收
- [ ] 卡片动画流畅
- [ ] 自动滚动平滑
- [ ] 状态更新及时
- [ ] 继续对话正常

---

**测试完成后，两个路径的卡片都应该能够正常自动显示！** 🎉
