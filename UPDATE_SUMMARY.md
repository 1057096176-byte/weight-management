# 首页更新说明

## 本次更新内容

### 1. 布局调整
- ✅ 医生形象移到右侧，问候语在左侧（横向布局）
- ✅ 优化了卡片样式，使用白色背景和边框
- ✅ 保持响应式设计

### 2. 功能入口栏
- ✅ 在输入框上方添加了功能入口栏
- ✅ 包含三个快捷入口：
  - 今日打卡（蓝色）
  - 智能导诊（紫色）
  - 数据（绿色）
- ✅ 图标 + 文字的垂直布局
- ✅ 悬停效果和过渡动画

### 3. 智能回复优化
用户提问后，系统根据关键词智能匹配回复：

**支持的关键词：**
- 减重/减肥 → 科学减重建议
- 饮食/吃什么/食谱 → 健康饮食建议
- 运动/锻炼 → 运动方案推荐
- 打卡/记录 → 打卡功能介绍
- 导诊/咨询 → 智能导诊功能说明
- 其他 → 通用回复

每个回复都包含：
- 专业建议内容
- 结构化的要点列表
- 引导用户进行下一步操作

### 4. 语音功能完善
- ✅ 语音选项菜单（点击语音按钮显示）
- ✅ 两种语音模式：
  1. 发送语音：直接录音并发送
  2. 语音转文字：识别后可编辑再发送
- ✅ 录音状态动画提示
- ✅ 防止录音时操作输入框

### 5. 常见问题优化
三个常见问题按钮：
- "如何科学减重？"
- "今天吃什么比较健康？"
- "适合我的运动方案"

点击后自动发送问题，触发智能回复。

## 页面结构

```
首页 (/)
├── 顶部导航栏
│   ├── 左：菜单按钮（打开侧边栏）
│   ├── 中：标题
│   └── 右：占位
├── 主内容区（可滚动）
│   ├── 医生形象和问候语卡片
│   │   ├── 左：问候语和描述
│   │   └── 右：圆形医生头像
│   ├── 状态卡片
│   │   ├── 新用户：引导卡片 + 开始导诊按钮
│   │   └── 老用户：方案信息 + 进度条 + 快捷操作
│   ├── 常见问题卡片（3个快捷按钮）
│   └── 聊天消息列表
├── 功能入口栏
│   ├── 今日打卡
│   ├── 智能导诊
│   └── 数据
└── 底部输入框
    ├── 图片上传（可选）
    ├── 文本输入
    ├── 语音输入（带选项菜单）
    └── 发送按钮
```

## 交互流程

### 新用户流程
1. 看到欢迎卡片和引导话术
2. 查看AI助手能提供的功能
3. 点击"开始智能导诊"或常见问题
4. 通过输入框与AI对话

### 老用户流程
1. 看到个性化问候和方案状态
2. 查看今日任务完成度
3. 快速访问打卡或数据页面
4. 点击常见问题或自由提问

## 新增组件

### QuickActions 组件
- 位置：`/src/app/components/QuickActions.tsx`
- 功能：底部功能入口栏
- 特点：
  - 图标+文字垂直布局
  - 三个快捷入口
  - 悬停效果
  - 响应式设计

## 技术特点

1. **条件渲染**：根据用户状态显示不同界面
2. **关键词匹配**：智能回复系统
3. **动画效果**：使用 Motion 实现流畅动画
4. **响应式设计**：适配不同屏幕尺寸
5. **组件化**：功能模块化，易于维护

## 使用说明

### 切换用户状态
在 `/src/app/pages/Home.tsx` 中修改：
```typescript
const userStatus = "has_plan"; // 改为 "new" 查看新用户界面
```

### 自定义常见问题
修改 `commonQuestions` 数组：
```typescript
const commonQuestions = [
  "你的问题1",
  "你的问题2",
  "你的问题3"
];
```

### 添加更多智能回复
在 `handleSendMessage` 函数中添加关键词匹配：
```typescript
if (message.includes("你的关键词")) {
  replyMessage = "你的回复内容";
}
```

## 下一步建议

1. **接入真实 AI 对话 API**（如 OpenAI、Claude）
2. **实现真实的语音识别**（Web Speech API 或云服务）
3. **添加消息历史持久化**（Supabase）
4. **优化回复内容**（更专业的健康建议）
5. **添加图片识别**（识别食物并计算热量）
